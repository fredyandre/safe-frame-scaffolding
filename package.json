{
  "name": "safe-frame-scaffolding",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir public && cp index.html styles.css script.js public/ && cp -r assets public/"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
